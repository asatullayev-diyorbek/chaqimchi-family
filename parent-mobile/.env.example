# Copy to .env and set to your dev machine's LAN IP (or a tunnel URL like
# ngrok) so a phone on the same network can reach the Django server.
# Find your LAN IP with: ipconfig getifaddr en0   (macOS)
EXPO_PUBLIC_API_URL=http://192.168.1.100:8000
